# Reyaansh Chat Electron App

A simple Electron app that opens `https://reyaansh-chat.onrender.com` in a desktop window.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the app:
   ```bash
   npm start
   ```
