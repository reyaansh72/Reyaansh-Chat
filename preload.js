// Preload can safely expose APIs to the renderer if needed.
// No APIs are currently exposed for this simple wrapper.

window.addEventListener('DOMContentLoaded', () => {
  // placeholder for future use
});
