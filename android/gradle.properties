org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
# When using the Kotlin DSL build files, enable the new DSL
# AGP 9+ only supports the new DSL; some Flutter plugin versions
# may be incompatible. Opt out for now to avoid the class-cast
# error while CI still uses an older Flutter plugin.
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
# Disable built-in Kotlin to fall back to the legacy Kotlin Gradle Plugin
# Some plugins still expect KGP on the classpath; reverting this flag
# lets the build use the traditional KGP behavior until dependencies
# or Flutter are updated.
android.builtInKotlin=false
